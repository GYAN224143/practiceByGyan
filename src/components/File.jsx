const File = () => {
  return (
    <div>
      this is file this is file this is file this is file this is file this is
      file this is file this is file this is file this is file this is file
    </div>
  );
};

export default File;
